# archive/experiments/ledgers

JSONL-ledgers som legat på fel path (t.ex. `experiments/*.jsonl` före `experiments/results/`).

Lägg undermappar per flytt, t.ex. `2026-05-28-pre-results-migration/`.

**Canonical aktiv källa:** `experiments/results/*.jsonl`.
