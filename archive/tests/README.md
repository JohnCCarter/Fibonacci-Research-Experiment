# archive/tests

Duplicerade eller ersatta testfiler från före `tests/`-speglar `src/fibengine/`.

Skapa undermapp per flytt (t.ex. `pre-structure-migration/`). **Canonical tester** ligger endast under `tests/<modul>/`.
