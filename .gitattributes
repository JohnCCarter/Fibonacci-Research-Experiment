# Shell scripts (incl. Claude Code hooks under .claude/hooks/) MUST stay LF — Git Bash chokes on
# a CRLF shebang/line endings on a fresh checkout, silently breaking the hooks across machines.
*.sh text eol=lf
